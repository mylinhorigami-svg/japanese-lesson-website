const lesson4Vocab = [
  {
    kana: "おきます",
    kanji: "起きます",
    romaji: "okimasu",
    meaning: "Thức dậy"
  },
  {
    kana: "ねます",
    kanji: "寝ます",
    romaji: "nemasu",
    meaning: "Ngủ"
  },
  {
    kana: "はたらきます",
    kanji: "働きます",
    romaji: "hatarakimasu",
    meaning: "Làm việc"
  },
  {
    kana: "やすみます",
    kanji: "休みます",
    romaji: "yasumimasu",
    meaning: "Nghỉ, nghỉ ngơi"
  },
  {
    kana: "べんきょうします",
    kanji: "勉強します",
    romaji: "benkyoushimasu",
    meaning: "Học"
  },
  {
    kana: "はじめます",
    kanji: "始めます",
    romaji: "hajimemasu",
    meaning: "Bắt đầu"
  },
  {
    kana: "おわります",
    kanji: "終わります",
    romaji: "owarimasu",
    meaning: "Kết thúc"
  },
  {
    kana: "デパート",
    kanji: "",
    romaji: "depaato",
    meaning: "Cửa hàng bách hóa"
  },
  {
    kana: "ぎんこう",
    kanji: "銀行",
    romaji: "ginkou",
    meaning: "Ngân hàng"
  },
  {
    kana: "ゆうびんきょく",
    kanji: "郵便局",
    romaji: "yuubinkyoku",
    meaning: "Bưu điện"
  },
  {
    kana: "としょかん",
    kanji: "図書館",
    romaji: "toshokan",
    meaning: "Thư viện"
  },
  {
    kana: "びじゅつかん",
    kanji: "美術館",
    romaji: "bijutsukan",
    meaning: "Bảo tàng mỹ thuật"
  },
  {
    kana: "えき",
    kanji: "駅",
    romaji: "eki",
    meaning: "Ga"
  },
  {
    kana: "プール",
    kanji: "",
    romaji: "puuru",
    meaning: "Bể bơi"
  },
  
  {
    kana: "げつようび",
    kanji: "月曜日",
    romaji: "getsuyoubi",
    meaning: "Thứ Hai"
  },
  {
    kana: "かようび",
    kanji: "火曜日",
    romaji: "kayoubi",
    meaning: "Thứ Ba"
  },
  {
    kana: "すいようび",
    kanji: "水曜日",
    romaji: "suiyoubi",
    meaning: "Thứ Tư"
  },
  {
    kana: "もくようび",
    kanji: "木曜日",
    romaji: "mokuyoubi",
    meaning: "Thứ Năm"
  },
  {
    kana: "きんようび",
    kanji: "金曜日",
    romaji: "kinyoubi",
    meaning: "Thứ Sáu"
  },
  {
    kana: "どようび",
    kanji: "土曜日",
    romaji: "doyoubi",
    meaning: "Thứ Bảy"
  },
  {
    kana: "にちようび",
    kanji: "日曜日",
    romaji: "nichiyoubi",
    meaning: "Chủ nhật"
  },
];
const lesson4Table = [
  {
    kana: "いま",
    kanji: "今",
    romaji: "ima",
    meaning: "Bây giờ"
  },
  {
    kana: "じ",
    kanji: "時",
    romaji: "ji",
    meaning: "Giờ"
  },
  {
    kana: "ふん(ぷん)",
    kanji: "分",
    romaji: "fun(pun)",
    meaning: "Phút"
  },
  {
    kana: "はん",
    kanji: "半",
    romaji: "han",
    meaning: "Rưỡi, 1 nửa"
  },
  {
    kana: "あさ",
    kanji: "朝",
    romaji: "asa",
    meaning: "Sáng"
  },
  {
    kana: "ひる",
    kanji: "昼",
    romaji: "hiru",
    meaning: "Trưa"
  },
  {
    kana: "ばん（よる）",
    kanji: "晩（夜）",
    romaji: "ban (yoru)",
    meaning: "Tối"
  },
  {
    kana: "ごぜん",
    kanji: "午前",
    romaji: "gozen",
    meaning: "Buổi sáng (6:00-12:00)"
  },
  {
    kana: "ごご",
    kanji: "午後",
    romaji: "gogo",
    meaning: "Buổi chiều (12:00-18:00)"
  },
  {
    kana: "おととい",
    kanji: "",
    romaji: "ototoi",
    meaning: "Hôm kia"
  },
  {
    kana: "きのう",
    kanji: "昨日",
    romaji: "kinou",
    meaning: "Hôm qua"
  },
  {
    kana: "きょう",
    kanji: "今日",
    romaji: "kyou",
    meaning: "Hôm nay"
  },
  {
    kana: "あした",
    kanji: "明日",
    romaji: "ashita",
    meaning: "Ngày mai"
  },
  {
    kana: "あさって",
    kanji: "",
    romaji: "asatte",
    meaning: "Ngày kia"
  },
  {
    kana: "けさ",
    kanji: "今朝",
    romaji: "kesa",
    meaning: "Sáng nay"
  },
  {
    kana: "こんばん",
    kanji: "今晩",
    romaji: "konban",
    meaning: "Tối nay"
  },
  {
    kana: "まいあさ",
    kanji: "毎朝",
    romaji: "maiasa",
    meaning: "Mỗi sáng"
  },
  {
    kana: "まいばん",
    kanji: "毎晩",
    romaji: "maiban",
    meaning: "Mỗi tối"
  },
  {
    kana: "まいにち",
    kanji: "毎日",
    romaji: "mainichi",
    meaning: "Hàng ngày"
  },
  {
    kana: "やすみ",
    kanji: "休み",
    romaji: "yasumi",
    meaning: "Việc nghỉ ngơi"
  },
  {
    kana: "べんきょう",
    kanji: "勉強",
    romaji: "benkyu",
    meaning: "Việc học"
  },
  {
    kana: "ばんごう",
    kanji: "番号",
    romaji: "bangou",
    meaning: "Số"
  },
  {
    kana: "ニューヨーク",
    kanji: "",
    romaji: "NyuuYooku",
    meaning: "New York"
  },
  {
    kana: "ペキン",
    kanji: "",
    romaji: "Pekin",
    meaning: "Bắc Kinh"
  },
  {
    kana: "ロンドン",
    kanji: "",
    romaji: "Rondon",
    meaning: "London"
  },
  {
    kana: "バンコク",
    kanji: "",
    romaji: "Bankoku",
    meaning: "Bangkok"
  },
  {
    kana: "ロサンゼルス",
    kanji: "",
    romaji: "Rosanzerusu",
    meaning: "Los Angeles"
  }
];

// Mảng kết hợp cho flashcard
const allFlashcardData = [...lesson4Vocab, ...lesson4Table];
const lesson4PracticeAllWords = [...lesson4Vocab, ...lesson4Table];

function renderLesson4Vocab() {
  const grid = document.getElementById("lesson4VocabGrid");
  if (!grid) return;

  grid.innerHTML = "";

  lesson4Vocab.forEach(item => {
    const card = document.createElement("div");
    card.className = "vocab-card";

    const kanjiHtml = item.kanji
      ? `<div class="vocab-kanji">${item.kanji}</div>`
      : "";

    card.innerHTML = `
      <img 
        class="vocab-img" 
        src="../../../../assets/images/lesson4/${item.romaji}.jpg" 
        alt="${item.kana}"
      >

      ${kanjiHtml}

      <div class="vocab-kana-row">

    <div class="vocab-kana">
        ${item.kana}
    </div>

    <button class="vocab-sound-btn"
            onclick="playVocabSound('${item.romaji}', this)">
        🔊
    </button>

</div>

      <div class="vocab-romaji">${item.romaji}</div>

      <div class="vocab-meaning">${item.meaning}</div>
    `;

    grid.appendChild(card);
  });
}

function renderLesson4Table() {
  const tableBody = document.getElementById("lesson4TableBody");
  if (!tableBody) return;

  tableBody.innerHTML = "";

  lesson4Table.forEach(item => {
    const row = document.createElement("tr");

    const kanjiCell = item.kanji ? item.kanji : "&nbsp;";

    row.innerHTML = `
      <td class="table-kana">${item.kana}</td>
      <td class="table-kanji">${kanjiCell}</td>
      <td class="table-romaji">${item.romaji}</td>
      <td class="table-meaning">${item.meaning}</td>
      <td>
        <button class="table-sound-btn"
                onclick="playVocabSound('${item.romaji}', this)">
          🔊
        </button>
      </td>
    `;

    tableBody.appendChild(row);
  });
}

function playVocabSound(romaji, btn) {
  const audio = new Audio(`../../../../assets/n5_vocab_audio/lesson4/${romaji}.mp3`);

  btn.classList.add("playing");

  audio.play();

  audio.onended = () => {
    btn.classList.remove("playing");
  };

  audio.onerror = () => {
    btn.classList.remove("playing");
    alert("Không tìm thấy file âm thanh: " + romaji + ".mp3");
  };
}

function playFlashcardSound(romaji) {
  if (!romaji) return;

  const audio = new Audio(`../../../../assets/n5_vocab_audio/lesson4/${romaji}.mp3`);

  audio.play().catch(() => {
    console.log("Không phát được âm thanh flashcard:", romaji);
  });
}

let flashcardIndex = 0;
let flashcardFlipped = false;
let flashcardVisible = false;

function renderFlashcard() {
  const content = document.getElementById("flashcardContent");
  const hintEl = document.getElementById("flashcardHint");
  const container = document.getElementById("flashcardContainer");

  if (!content || !container || !hintEl) return;
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  const item = allFlashcardData[flashcardIndex];

  flashcardFlipped = false;
  flashcardVisible = true;
  container.hidden = false;
  const counter = document.getElementById("flashcardCounter");

    if (counter) {
    counter.textContent = `${flashcardIndex + 1} / ${allFlashcardData.length}`;
    }
  content.innerHTML = `
    <div class="flashcard-face">
      
      <div class="flashcard-main">
        ${item.kana}
      </div>
    </div>
  `;

  hintEl.textContent = "Nhấn để lật thẻ";

  playFlashcardSound(item.romaji);
}

function toggleFlashcard() {
  const content = document.getElementById("flashcardContent");
  const hintEl = document.getElementById("flashcardHint");

  if (!content || !hintEl) return;
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  const item = allFlashcardData[flashcardIndex];

  if (!flashcardFlipped) {
    content.innerHTML = `
      <div class="flashcard-face">

        <div class="flashcard-kanji">
         ${item.kanji || ""}
        </div>

        <div class="flashcard-kana">
          ${item.kana}
        </div>

        <div class="flashcard-romaji">
          ${item.romaji}
        </div>

        <div class="flashcard-meaning">
          ${item.meaning}
        </div>
      </div>
    `;

    hintEl.textContent = "Nhấn để quay lại";
    flashcardFlipped = true;

  } else {
    content.innerHTML = `
      <div class="flashcard-face">

        <div class="flashcard-main">
          ${item.kana}
        </div>
      </div>
    `;

    hintEl.textContent = "Nhấn để lật thẻ";
    flashcardFlipped = false;
  }
}

function changeFlashcard(step) {
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  flashcardIndex =
    (flashcardIndex + step + allFlashcardData.length) % allFlashcardData.length;

  renderFlashcard();
}

function initFlashcards() {
  const toggleBtn = document.getElementById("flashcardToggleBtn");
  const container = document.getElementById("flashcardContainer");
  const card = document.getElementById("flashcardCard");
  const prevBtn = document.getElementById("flashcardPrev");
  const nextBtn = document.getElementById("flashcardNext");

  if (!toggleBtn || !container || !card || !prevBtn || !nextBtn) return;

  container.hidden = true;

  toggleBtn.addEventListener("click", () => {
    if (flashcardVisible) {
      flashcardVisible = false;
      container.hidden = true;
      toggleBtn.textContent = "🃏 Flashcard";
    } else {
      renderFlashcard();
      toggleBtn.textContent = "🃏 Flashcard";
    }
  });

  card.addEventListener("click", toggleFlashcard);

  card.addEventListener("keydown", (event) => {
    if (event.key === "Enter" || event.key === " ") {
      event.preventDefault();
      toggleFlashcard();
    }

    if (event.key === "ArrowLeft") {
      event.preventDefault();
      changeFlashcard(-1);
    }

    if (event.key === "ArrowRight") {
      event.preventDefault();
      changeFlashcard(1);
    }
  });

  prevBtn.addEventListener("click", (event) => {
    event.stopPropagation();
    changeFlashcard(-1);
  });

  nextBtn.addEventListener("click", (event) => {
    event.stopPropagation();
    changeFlashcard(1);
  });
}

/*Practice*/
const lesson4PracticeWords = {
  1: allFlashcardData.slice(0, 10),
  2: allFlashcardData.slice(10, 20),
  3: allFlashcardData.slice(20, 30),
  4: allFlashcardData.slice(30, 40),
  5: allFlashcardData.slice(40, 47)
};
let currentPracticeNumber = null;
let currentPracticeQuestions = [];
let currentPracticeQuestionIndex = 0;
let currentPracticeScore = 0;
let currentPracticePanel = null;
let lesson4AnswerLocked = false;

const lesson4PracticeAudio = new Audio();
function shuffleLesson4Array(array) {
  const newArray = [...array];

  for (let i = newArray.length - 1; i > 0; i--) {
    const randomIndex = Math.floor(
      Math.random() * (i + 1)
    );

    [newArray[i], newArray[randomIndex]] = [
      newArray[randomIndex],
      newArray[i]
    ];
  }

  return newArray;
}
function createLesson4PracticeAnswers(
  correctWord,
  answerType
) {
  const correctAnswer =
    answerType === "meaning"
      ? correctWord.meaning
      : correctWord.kana;

  const wrongAnswers = allFlashcardData
    .filter(word => {
      return word.kana !== correctWord.kana;
    })
    .map(word => {
      return answerType === "meaning"
        ? word.meaning
        : word.kana;
    })
    .filter((answer, index, array) => {
      return (
        answer &&
        answer !== correctAnswer &&
        array.indexOf(answer) === index
      );
    });

  const selectedWrongAnswers =
    shuffleLesson4Array(wrongAnswers).slice(0, 3);

  return shuffleLesson4Array([
    correctAnswer,
    ...selectedWrongAnswers
  ]);
}
function createLesson4PracticeQuestions(practiceNumber) {
  const words = lesson4PracticeWords[practiceNumber];

  if (!words || words.length === 0) {
    return [];
  }

  const questions = [];

  const shuffledWords = shuffleLesson4Array([
    ...words
  ]);

  const kanaWords = shuffledWords.slice(
    0,
    Math.min(8, shuffledWords.length)
  );

  const meaningWords = shuffleLesson4Array([
    ...words
  ]).slice(
    0,
    Math.min(8, words.length)
  );

  const audioWords = shuffleLesson4Array([
    ...words
  ]).slice(
    0,
    Math.min(4, words.length)
  );

  kanaWords.forEach(word => {
    questions.push({
      type: "kanaToMeaning",
      question: word.kana,
      correct: word.meaning,
      answers: createLesson4PracticeAnswers(
        word,
        "meaning"
      ),
      word: word
    });
  });

  meaningWords.forEach(word => {
    questions.push({
      type: "meaningToKana",
      question: word.meaning,
      correct: word.kana,
      answers: createLesson4PracticeAnswers(
        word,
        "kana"
      ),
      word: word
    });
  });

  audioWords.forEach(word => {
    questions.push({
      type: "audioToMeaning",
      question: "",
      correct: word.meaning,
      answers: createLesson4PracticeAnswers(
        word,
        "meaning"
      ),
      word: word
    });
  });

  return shuffleLesson4Array(questions);
}
function togglePractice(practiceNumber, button) {
  if (!button) {
    const btns = document.querySelectorAll(".practice-title-btn");
    button = btns[Number(practiceNumber) - 1] || btns[0];
  }

  if (!button) return;

  const practiceItem = button.closest(".practice-item");
  if (!practiceItem) return;

  const panel = practiceItem.querySelector(".practice-panel");
  if (!panel) return;

  const isOpen = panel.classList.contains("open");

  lesson4PracticeAudio.pause();
  lesson4PracticeAudio.currentTime = 0;

  document.querySelectorAll(".practice-panel").forEach(item => {
    item.classList.remove("open");
    item.innerHTML = "";
  });

  document.querySelectorAll(".practice-title-btn").forEach((practiceButton, index) => {
    practiceButton.classList.remove("active");
    practiceButton.textContent = `📝 Luyện tập ${index + 1}`;
  });

  if (isOpen) {
    currentPracticePanel = null;
    currentPracticeNumber = null;
    return;
  }

  currentPracticeNumber = Number(practiceNumber);
  currentPracticePanel = panel;

  button.classList.add("active");
  button.textContent = `▼ Luyện tập ${currentPracticeNumber}`;

  panel.classList.add("open");

  startLesson4Practice();
}

function startLesson4Practice() {
  currentPracticeQuestions =
    createLesson4PracticeQuestions(
      currentPracticeNumber
    );

  currentPracticeQuestionIndex = 0;
  currentPracticeScore = 0;
  lesson4AnswerLocked = false;

  showLesson4PracticeQuestion();
}
function showLesson4PracticeQuestion() {
  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  if (!question) {
    showLesson4PracticeResult();
    return;
  }

  lesson4AnswerLocked = false;

  lesson4PracticeAudio.pause();
  lesson4PracticeAudio.currentTime = 0;

  const totalQuestions =
    currentPracticeQuestions.length;

  const questionNumber =
    currentPracticeQuestionIndex + 1;

  const progressPercent =
    (questionNumber / totalQuestions) * 100;


  let instruction = "";

  if (question.type === "kanaToMeaning") {
    instruction =
      "Chọn đáp án đúng";
  }

  if (question.type === "meaningToKana") {
    instruction =
      "Chọn đáp án đúng";
  }

  if (question.type === "audioToMeaning") {
    instruction =
      "Chọn đáp án đúng";
  }


  let questionHTML = `
    <div class="practice-question-text">
      ${question.question}
    </div>
  `;


  if (question.type === "audioToMeaning") {
    questionHTML = `
      <div class="practice-audio-area">

        <button
          class="practice-audio-btn"
          onclick="playLesson4PracticeQuestionAudio(this)"
        >
          <span class="practice-speaker">
            🔊
          </span>
        </button>

      </div>
    `;
  }


  const answersHTML = question.answers
    .map((answer, index) => {
      return `
        <button
          class="practice-answer-btn"
          data-answer="${encodeURIComponent(answer)}"
          onclick="checkLesson4PracticeAnswer(this)"
        >

          <span class="answer-letter">
            ${String.fromCharCode(65 + index)}
          </span>

          <span class="answer-text">
            ${answer}
          </span>

        </button>
      `;
    })
    .join("");


  currentPracticePanel.innerHTML = `
    <div class="practice-box">

      <div class="practice-box-title">
        Từ vựng bài 4
      </div>

      <div class="practice-progress-info">

        <span>
          Câu ${questionNumber}/${totalQuestions}
        </span>

        <span>
          Điểm: ${currentPracticeScore}
        </span>

      </div>

      <div class="practice-progress-track">

        <div
          class="practice-progress-fill"
          style="width: ${progressPercent}%"
        >
        </div>

      </div>

      <div class="practice-content">

        <div class="practice-instruction">
          ${instruction}
        </div>

        ${questionHTML}

        <div class="practice-answer-list">
          ${answersHTML}
        </div>

        <div
          id="practiceFeedback"
          class="practice-feedback"
        >
        </div>

        <div
          id="practiceNextArea"
          class="practice-next-area"
        >
        </div>

      </div>

    </div>
  `;
   if (question.type === "audioToMeaning") {
    setTimeout(() => {
      playLesson4PracticeQuestionAudio();
    }, 500);
  }
}
function checkLesson4PracticeAnswer(button) {
  if (lesson4AnswerLocked) {
    return;
  }

  lesson4AnswerLocked = true;

  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  const selectedAnswer = decodeURIComponent(
    button.dataset.answer
  );

  const answerButtons =
    currentPracticePanel.querySelectorAll(
      ".practice-answer-btn"
    );


  answerButtons.forEach(answerButton => {
    answerButton.disabled = true;

    const answer = decodeURIComponent(
      answerButton.dataset.answer
    );

    if (answer === question.correct) {
      answerButton.classList.add("correct");
    }
  });


  const feedback =
    currentPracticePanel.querySelector(
      "#practiceFeedback"
    );


  if (selectedAnswer === question.correct) {
    currentPracticeScore++;

    button.classList.add("correct");

    feedback.innerHTML = `
      <div class="feedback-correct">
        😊🎉
      </div>
    `;
  } else {
    button.classList.add("wrong");

    feedback.innerHTML = `
      <div class="feedback-wrong">
       😢
      </div>
    `;
  }


  const nextArea =
    currentPracticePanel.querySelector(
      "#practiceNextArea"
    );

  const isLastQuestion =
  currentPracticeQuestionIndex ===
  currentPracticeQuestions.length - 1;

if (isLastQuestion) {

  // Hiện kết quả sau 1 giây
  setTimeout(() => {
    showLesson4PracticeResult();
  }, 1000);

} else {

  nextArea.innerHTML = `
    <button
      class="practice-next-btn"
      onclick="nextLesson4PracticeQuestion()"
    >
      Next
    </button>
  `;
}
}
function nextLesson4PracticeQuestion() {
  lesson4PracticeAudio.pause();
  lesson4PracticeAudio.currentTime = 0;

  currentPracticeQuestionIndex++;

  if (
    currentPracticeQuestionIndex >=
    currentPracticeQuestions.length
  ) {
    showLesson4PracticeResult();
    return;
  }

  showLesson4PracticeQuestion();
}
function showLesson4PracticeResult() {
  lesson4PracticeAudio.pause();
  lesson4PracticeAudio.currentTime = 0;

  const totalQuestions =
    currentPracticeQuestions.length;

  const percent = Math.round(
    (currentPracticeScore / totalQuestions) * 100
  );

  let resultEmoji = "";
let resultMessage = "";

if (percent === 100) {
    resultEmoji = "🏆";
    resultMessage = "Xuất sắc!";
}
else if (percent >= 80) {
    resultEmoji = "🎉";
    resultMessage = "Tuyệt vời!";
}
else if (percent >= 60) {
    resultEmoji = "👍";
    resultMessage = "Rất tốt!";
}
else {
    resultEmoji = "💪";
    resultMessage = "Cố lên!";
}

  currentPracticePanel.innerHTML = `
    <div class="practice-box">

      <div class="practice-box-title">
        Kết quả luyện tập ${currentPracticeNumber}
      </div>

      <div class="practice-result">

        <div class="practice-result-icon">
          ${resultEmoji}
        </div>

        <div class="practice-result-score">
          ${currentPracticeScore}/${totalQuestions}
        </div>

        <div class="practice-result-message">
          ${resultMessage}
        </div>

        <div class="practice-result-buttons">

          <button
            class="practice-restart-btn"
            onclick="startLesson4Practice()"
          >
            Làm lại
          </button>

          <button
            class="practice-close-btn"
            onclick="closeLesson4Practice()"
          >
            Đóng
          </button>

        </div>

      </div>

    </div>
  `;
}
function closeLesson4Practice() {
  lesson4PracticeAudio.pause();
  lesson4PracticeAudio.currentTime = 0;

  if (!currentPracticePanel) {
    return;
  }

  const practiceItem =
    currentPracticePanel.closest(".practice-item");

  const button =
    practiceItem.querySelector(
      ".practice-title-btn"
    );

  currentPracticePanel.classList.remove("open");
  currentPracticePanel.innerHTML = "";

  button.classList.remove("active");
  button.textContent =
    `▶ Luyện tập ${currentPracticeNumber}`;

  currentPracticePanel = null;
  currentPracticeNumber = null;
}
function playLesson4PracticeQuestionAudio(
  button = null
) {
  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  if (!question || !question.word) {
    return;
  }

  const romaji = question.word.romaji;

  if (!romaji) {
    console.log(
      "Từ này chưa có dữ liệu romaji."
    );

    return;
  }

  lesson4PracticeAudio.pause();
  lesson4PracticeAudio.currentTime = 0;

  lesson4PracticeAudio.src =
    `../../../../assets/n5_vocab_audio/lesson4/${romaji}.mp3`;


  if (button) {
    button.classList.add("playing");
  }


  lesson4PracticeAudio
    .play()
    .catch(error => {
      console.log(
        "Không phát được âm thanh:",
        romaji,
        error
      );

      if (button) {
        button.classList.remove("playing");
      }
    });


  lesson4PracticeAudio.onended = () => {
    if (button) {
      button.classList.remove("playing");
    }
  };

  lesson4PracticeAudio.onerror = () => {
    if (button) {
      button.classList.remove("playing");
    }

    console.log(
      `Không tìm thấy file: ${romaji}.mp3`
    );
  };
}

document.addEventListener("DOMContentLoaded", () => {
  renderLesson4Vocab();
  renderLesson4Table();
  initFlashcards();
});