const lesson7Vocab = [
  {
    kana: "て",
    kanji: "手",
    romaji: "te",
    meaning: "Tay, bàn tay"
  },
  {
    kana: "はし",
    kanji: "",
    romaji: "hashi",
    meaning: "Đũa, đôi đũa"
  },
  {
    kana: "ナイフ",
    kanji: "",
    romaji: "naifu",
    meaning: "Dao, con dao"
  },
  {
    kana: "フォーク",
    kanji: "",
    romaji: "fooku",
    meaning: "Cái dĩa, cái nĩa"
  },
  {
    kana: "スプーン",
    kanji: "",
    romaji: "supuun",
    meaning: "Cái thìa, cái muỗng"
  },
  {
    kana: "はさみ",
    kanji: "",
    romaji: "hasami",
    meaning: "Cái kéo"
  },
  {
    kana: "ドライバー",
    kanji: "",
    romaji: "doraibaa",
    meaning: "Cái tuốc-lơ-vít"
  },
  {
    kana: "スパナ",
    kanji: "",
    romaji: "supana",
    meaning: "Cái mỏ lết"
  },
  {
    kana: "ペンチ",
    kanji: "",
    romaji: "penchi",
    meaning: "Cái kìm"
  },
  {
    kana: "ホッチキス",
    kanji: "",
    romaji: "hotchikisu",
    meaning: "Cái dập ghim"
  },
  {
    kana: "けしゴム",
    kanji: "消しゴム",
    romaji: "keshigomu",
    meaning: "Cục tẩy"
  },
  {
    kana: "おかね",
    kanji: "お金",
    romaji: "okane",
    meaning: "Tiền"
  },
  {
    kana: "プレゼント",
    kanji: "",
    romaji: "purezento",
    meaning: "Quà, món quà"
  },
  {
    kana: "はな",
    kanji: "花",
    romaji: "hana",
    meaning: "Hoa, bông hoa"
  },
  {
    kana: "にもつ",
    kanji: "荷物",
    romaji: "nimotsu",
    meaning: "Hành lý, đồ đạc"
  },
  {
    kana: "きっぷ",
    kanji: "切符",
    romaji: "kippu",
    meaning: "Vé, tấm vé"
  },
  {
    kana: "パソコン",
    kanji: "",
    romaji: "pasokon",
    meaning: "Máy tính cá nhân"
  },
  {
    kana: "クリスマス",
    kanji: "",
    romaji: "kurisumasu",
    meaning: "Giáng sinh"
  },
  {
    kana: "おみやげ",
    kanji: "お土産",
    romaji: "omiyage",
    meaning: "Quà lưu niệm, đặc sản"
  },
  {
    kana: "スペイン",
    kanji: "",
    romaji: "supein",
    meaning: "Tây Ban Nha"
  }
];
const lesson7Table = [
  {
    kana: "きります",
    kanji: "切ります",
    romaji: "kirimasu",
    meaning: "Cắt, gọt"
  },
  {
    kana: "しゅうりします",
    kanji: "修理します",
    romaji: "shuurishimasu",
    meaning: "Sửa chữa"
  },
  {
    kana: "かけます",
    kanji: "",
    romaji: "kakemasu",
    meaning: "Gọi (điện thoại)"
  },
  {
    kana: "あげます",
    kanji: "",
    romaji: "agemasu",
    meaning: "Cho, biếu, tặng"
  },
  {
    kana: "もらいます",
    kanji: "",
    romaji: "moraimasu",
    meaning: "Nhận, nhận được"
  },
  {
    kana: "おしえます",
    kanji: "教えます",
    romaji: "oshiemasu",
    meaning: "Dạy, chỉ dẫn"
  },
  {
    kana: "ならいます",
    kanji: "習います",
    romaji: "naraimasu",
    meaning: "Học được"
  },
  {
    kana: "かします",
    kanji: "貸します",
    romaji: "kashimasu",
    meaning: "Cho mượn, cho vay, cho thuê"
  },
  {
    kana: "かります",
    kanji: "借ります",
    romaji: "karimasu",
    meaning: "Mượn, vay, thuê"
  },
  {
    kana: "おくります",
    kanji: "送ります",
    romaji: "okurimasu",
    meaning: "Gửi"
  },
  {
    kana: "ちち",
    kanji: "父",
    romaji: "chichi",
    meaning: "Bố của mình"
  },
  {
    kana: "おとうさん",
    kanji: "お父さん",
    romaji: "otousan",
    meaning: "Bố của người khác"
  },
  {
    kana: "はは",
    kanji: "母",
    romaji: "haha",
    meaning: "Mẹ của mình"
  },
  {
    kana: "おかあさん",
    kanji: "お母さん",
    romaji: "okaasan",
    meaning: "Mẹ của người khác"
  },
  {
    kana: "あに",
    kanji: "兄",
    romaji: "ani",
    meaning: "Anh trai của mình"
  },
  {
    kana: "おにいさん",
    kanji: "お兄さん",
    romaji: "oniisan",
    meaning: "Anh trai của người khác"
  },
  {
    kana: "あね",
    kanji: "姉",
    romaji: "ane",
    meaning: "Chị gái của mình"
  },
  {
    kana: "おねえさん",
    kanji: "お姉さん",
    romaji: "oneesan",
    meaning: "Chị gái của người khác"
  },
  {
    kana: "おとうと",
    kanji: "弟",
    romaji: "otouto",
    meaning: "Em trai của mình"
  },
  {
    kana: "おとうとさん",
    kanji: "弟さん",
    romaji: "otoutosan",
    meaning: "Em trai của người khác"
  },
  {
    kana: "いもうと",
    kanji: "妹",
    romaji: "imouto",
    meaning: "Em gái của mình"
  },
  {
    kana: "いもうとさん",
    kanji: "妹さん",
    romaji: "imoutosan",
    meaning: "Em gái của người khác"
  },
  {
    kana: "つま",
    kanji: "妻",
    romaji: "tsuma",
    meaning: "Vợ của mình"
  },
  {
    kana: "おくさん",
    kanji: "奥さん",
    romaji: "okusan",
    meaning: "Vợ của người khác"
  },
  {
    kana: "しゅじん",
    kanji: "主人",
    romaji: "shujin",
    meaning: "Chồng của mình"
  },
  {
    kana: "ごしゅじん",
    kanji: "ご主人",
    romaji: "goshujin",
    meaning: "Chồng của người khác"
  },
  {
    kana: "こども",
    kanji: "子供",
    romaji: "kodomo",
    meaning: "Trẻ con, con cái của mình"
  },
  {
    kana: "おこさん",
    kanji: "お子さん",
    romaji: "okosan",
    meaning: "Con cái của người khác" 
  },
  {
    kana: "りょこう",
    kanji: "旅行",
    romaji: "ryokou",
    meaning: "Du lịch"
  }
];

// Mảng kết hợp cho flashcard
const allFlashcardData = [...lesson7Vocab, ...lesson7Table];
const lesson7PracticeAllWords = [...lesson7Vocab, ...lesson7Table];

function renderLesson7Vocab() {
  const grid = document.getElementById("lesson7VocabGrid");
  if (!grid) return;

  grid.innerHTML = "";

  lesson7Vocab.forEach(item => {
    const card = document.createElement("div");
    card.className = "vocab-card";

    const kanjiHtml = item.kanji
      ? `<div class="vocab-kanji">${item.kanji}</div>`
      : "";

    card.innerHTML = `
      <img 
        class="vocab-img" 
        src="../../../../assets/images/lesson7/${item.romaji}.jpg" 
        alt="${item.kana}"
      >

      ${kanjiHtml}

      <div class="vocab-kana-row">

    <div class="vocab-kana">
        ${item.kana}
    </div>

    <button class="vocab-sound-btn"
            onclick="playVocabSound('${item.romaji}', this)">
        🔊
    </button>

</div>

      <div class="vocab-romaji">${item.romaji}</div>

      <div class="vocab-meaning">${item.meaning}</div>
    `;

    grid.appendChild(card);
  });
}

function renderLesson7Table() {
  const tableBody = document.getElementById("lesson7TableBody");
  if (!tableBody) return;

  tableBody.innerHTML = "";

  lesson7Table.forEach(item => {
    const row = document.createElement("tr");

    const kanjiCell = item.kanji ? item.kanji : "&nbsp;";

    row.innerHTML = `
      <td class="table-kana">${item.kana}</td>
      <td class="table-kanji">${kanjiCell}</td>
      <td class="table-romaji">${item.romaji}</td>
      <td class="table-meaning">${item.meaning}</td>
      <td>
        <button class="table-sound-btn"
                onclick="playVocabSound('${item.romaji}', this)">
          🔊
        </button>
      </td>
    `;

    tableBody.appendChild(row);
  });
}

function playVocabSound(romaji, btn) {
  const audio = new Audio(`../../../../assets/audio/lesson7/${romaji}.mp3`);

  btn.classList.add("playing");

  audio.play();

  audio.onended = () => {
    btn.classList.remove("playing");
  };

  audio.onerror = () => {
    btn.classList.remove("playing");
    alert("Không tìm thấy file âm thanh: " + romaji + ".mp3");
  };
}

function playFlashcardSound(romaji) {
  if (!romaji) return;

  const audio = new Audio(`../../../../assets/audio/lesson7/${romaji}.mp3`);

  audio.play().catch(() => {
    console.log("Không phát được âm thanh flashcard:", romaji);
  });
}

let flashcardIndex = 0;
let flashcardFlipped = false;
let flashcardVisible = false;

function renderFlashcard() {
  const content = document.getElementById("flashcardContent");
  const hintEl = document.getElementById("flashcardHint");
  const container = document.getElementById("flashcardContainer");

  if (!content || !container || !hintEl) return;
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  const item = allFlashcardData[flashcardIndex];

  flashcardFlipped = false;
  flashcardVisible = true;
  container.hidden = false;
  const counter = document.getElementById("flashcardCounter");

    if (counter) {
    counter.textContent = `${flashcardIndex + 1} / ${allFlashcardData.length}`;
    }
  content.innerHTML = `
    <div class="flashcard-face">
      
      <div class="flashcard-main">
        ${item.kana}
      </div>
    </div>
  `;

  hintEl.textContent = "Nhấn để lật thẻ";

  playFlashcardSound(item.romaji);
}

function toggleFlashcard() {
  const content = document.getElementById("flashcardContent");
  const hintEl = document.getElementById("flashcardHint");

  if (!content || !hintEl) return;
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  const item = allFlashcardData[flashcardIndex];

  if (!flashcardFlipped) {
    content.innerHTML = `
      <div class="flashcard-face">

        <div class="flashcard-kanji">
         ${item.kanji || ""}
        </div>

        <div class="flashcard-kana">
          ${item.kana}
        </div>

        <div class="flashcard-romaji">
          ${item.romaji}
        </div>

        <div class="flashcard-meaning">
          ${item.meaning}
        </div>
      </div>
    `;

    hintEl.textContent = "Nhấn để quay lại";
    flashcardFlipped = true;

  } else {
    content.innerHTML = `
      <div class="flashcard-face">

        <div class="flashcard-main">
          ${item.kana}
        </div>
      </div>
    `;

    hintEl.textContent = "Nhấn để lật thẻ";
    flashcardFlipped = false;
  }
}

function changeFlashcard(step) {
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  flashcardIndex =
    (flashcardIndex + step + allFlashcardData.length) % allFlashcardData.length;

  renderFlashcard();
}

function initFlashcards() {
  const toggleBtn = document.getElementById("flashcardToggleBtn");
  const container = document.getElementById("flashcardContainer");
  const card = document.getElementById("flashcardCard");
  const prevBtn = document.getElementById("flashcardPrev");
  const nextBtn = document.getElementById("flashcardNext");

  if (!toggleBtn || !container || !card || !prevBtn || !nextBtn) return;

  container.hidden = true;

  toggleBtn.addEventListener("click", () => {
    if (flashcardVisible) {
      flashcardVisible = false;
      container.hidden = true;
      toggleBtn.textContent = "🃏 Flashcard";
    } else {
      renderFlashcard();
      toggleBtn.textContent = "🃏 Flashcard";
    }
  });

  card.addEventListener("click", toggleFlashcard);

  card.addEventListener("keydown", (event) => {
    if (event.key === "Enter" || event.key === " ") {
      event.preventDefault();
      toggleFlashcard();
    }

    if (event.key === "ArrowLeft") {
      event.preventDefault();
      changeFlashcard(-1);
    }

    if (event.key === "ArrowRight") {
      event.preventDefault();
      changeFlashcard(1);
    }
  });

  prevBtn.addEventListener("click", (event) => {
    event.stopPropagation();
    changeFlashcard(-1);
  });

  nextBtn.addEventListener("click", (event) => {
    event.stopPropagation();
    changeFlashcard(1);
  });
}

/*Practice*/
const lesson7PracticeWords = {
  1: allFlashcardData.slice(0, 10),
  2: allFlashcardData.slice(10, 20),
  3: allFlashcardData.slice(20, 30),
  4: allFlashcardData.slice(30, 40),
  5: allFlashcardData.slice(40, 49)
};
let currentPracticeNumber = null;
let currentPracticeQuestions = [];
let currentPracticeQuestionIndex = 0;
let currentPracticeScore = 0;
let currentPracticePanel = null;
let lesson7AnswerLocked = false;

const lesson7PracticeAudio = new Audio();
function shuffleLesson7Array(array) {
  const newArray = [...array];

  for (let i = newArray.length - 1; i > 0; i--) {
    const randomIndex = Math.floor(
      Math.random() * (i + 1)
    );

    [newArray[i], newArray[randomIndex]] = [
      newArray[randomIndex],
      newArray[i]
    ];
  }

  return newArray;
}
function createLesson7PracticeAnswers(
  correctWord,
  answerType
) {
  const correctAnswer =
    answerType === "meaning"
      ? correctWord.meaning
      : correctWord.kana;

  const wrongAnswers = allFlashcardData
    .filter(word => {
      return word.kana !== correctWord.kana;
    })
    .map(word => {
      return answerType === "meaning"
        ? word.meaning
        : word.kana;
    })
    .filter((answer, index, array) => {
      return (
        answer &&
        answer !== correctAnswer &&
        array.indexOf(answer) === index
      );
    });

  const selectedWrongAnswers =
    shuffleLesson7Array(wrongAnswers).slice(0, 3);

  return shuffleLesson7Array([
    correctAnswer,
    ...selectedWrongAnswers
  ]);
}
function createLesson7PracticeQuestions(practiceNumber) {
  const words = lesson7PracticeWords[practiceNumber];

  if (!words || words.length === 0) {
    return [];
  }

  const questions = [];

  const shuffledWords = shuffleLesson7Array([
    ...words
  ]);

  const kanaWords = shuffledWords.slice(
    0,
    Math.min(8, shuffledWords.length)
  );

  const meaningWords = shuffleLesson7Array([
    ...words
  ]).slice(
    0,
    Math.min(8, words.length)
  );

  const audioWords = shuffleLesson7Array([
    ...words
  ]).slice(
    0,
    Math.min(4, words.length)
  );

  kanaWords.forEach(word => {
    questions.push({
      type: "kanaToMeaning",
      question: word.kana,
      correct: word.meaning,
      answers: createLesson7PracticeAnswers(
        word,
        "meaning"
      ),
      word: word
    });
  });

  meaningWords.forEach(word => {
    questions.push({
      type: "meaningToKana",
      question: word.meaning,
      correct: word.kana,
      answers: createLesson7PracticeAnswers(
        word,
        "kana"
      ),
      word: word
    });
  });

  audioWords.forEach(word => {
    questions.push({
      type: "audioToMeaning",
      question: "",
      correct: word.meaning,
      answers: createLesson7PracticeAnswers(
        word,
        "meaning"
      ),
      word: word
    });
  });

  return shuffleLesson7Array(questions);
}
function togglePractice(practiceNumber, button) {
  if (!button) {
    const btns = document.querySelectorAll(".practice-title-btn");
    button = btns[Number(practiceNumber) - 1] || btns[0];
  }

  if (!button) return;

  const practiceItem = button.closest(".practice-item");
  if (!practiceItem) return;

  const panel = practiceItem.querySelector(".practice-panel");
  if (!panel) return;

  const isOpen = panel.classList.contains("open");

  lesson7PracticeAudio.pause();
  lesson7PracticeAudio.currentTime = 0;

  document.querySelectorAll(".practice-panel").forEach(item => {
    item.classList.remove("open");
    item.innerHTML = "";
  });

  document.querySelectorAll(".practice-title-btn").forEach((practiceButton, index) => {
    practiceButton.classList.remove("active");
    practiceButton.textContent = `📝 Luyện tập ${index + 1}`;
  });

  if (isOpen) {
    currentPracticePanel = null;
    currentPracticeNumber = null;
    return;
  }

  currentPracticeNumber = Number(practiceNumber);
  currentPracticePanel = panel;

  button.classList.add("active");
  button.textContent = `▼ Luyện tập ${currentPracticeNumber}`;

  panel.classList.add("open");

  startLesson7Practice();
}

function startLesson7Practice() {
  currentPracticeQuestions =
    createLesson7PracticeQuestions(
      currentPracticeNumber
    );

  currentPracticeQuestionIndex = 0;
  currentPracticeScore = 0;
  lesson7AnswerLocked = false;

  showLesson7PracticeQuestion();
}
function showLesson7PracticeQuestion() {
  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  if (!question) {
    showLesson7PracticeResult();
    return;
  }

  lesson7AnswerLocked = false;

  lesson7PracticeAudio.pause();
  lesson7PracticeAudio.currentTime = 0;

  const totalQuestions =
    currentPracticeQuestions.length;

  const questionNumber =
    currentPracticeQuestionIndex + 1;

  const progressPercent =
    (questionNumber / totalQuestions) * 100;


  let instruction = "";

  if (question.type === "kanaToMeaning") {
    instruction =
      "Chọn đáp án đúng";
  }

  if (question.type === "meaningToKana") {
    instruction =
      "Chọn đáp án đúng";
  }

  if (question.type === "audioToMeaning") {
    instruction =
      "Chọn đáp án đúng";
  }


  let questionHTML = `
    <div class="practice-question-text">
      ${question.question}
    </div>
  `;


  if (question.type === "audioToMeaning") {
    questionHTML = `
      <div class="practice-audio-area">

        <button
          class="practice-audio-btn"
          onclick="playLesson7PracticeQuestionAudio(this)"
        >
          <span class="practice-speaker">
            🔊
          </span>
        </button>

      </div>
    `;
  }


  const answersHTML = question.answers
    .map((answer, index) => {
      return `
        <button
          class="practice-answer-btn"
          data-answer="${encodeURIComponent(answer)}"
          onclick="checkLesson7PracticeAnswer(this)"
        >

          <span class="answer-letter">
            ${String.fromCharCode(65 + index)}
          </span>

          <span class="answer-text">
            ${answer}
          </span>

        </button>
      `;
    })
    .join("");


  currentPracticePanel.innerHTML = `
    <div class="practice-box">

      <div class="practice-box-title">
        Từ vựng bài 7
      </div>

      <div class="practice-progress-info">

        <span>
          Câu ${questionNumber}/${totalQuestions}
        </span>

        <span>
          Điểm: ${currentPracticeScore}
        </span>

      </div>

      <div class="practice-progress-track">

        <div
          class="practice-progress-fill"
          style="width: ${progressPercent}%"
        >
        </div>

      </div>

      <div class="practice-content">

        <div class="practice-instruction">
          ${instruction}
        </div>

        ${questionHTML}

        <div class="practice-answer-list">
          ${answersHTML}
        </div>

        <div
          id="practiceFeedback"
          class="practice-feedback"
        >
        </div>

        <div
          id="practiceNextArea"
          class="practice-next-area"
        >
        </div>

      </div>

    </div>
  `;
   if (question.type === "audioToMeaning") {
    setTimeout(() => {
      playLesson7PracticeQuestionAudio();
    }, 500);
  }
}
function checkLesson7PracticeAnswer(button) {
  if (lesson7AnswerLocked) {
    return;
  }

  lesson7AnswerLocked = true;

  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  const selectedAnswer = decodeURIComponent(
    button.dataset.answer
  );

  const answerButtons =
    currentPracticePanel.querySelectorAll(
      ".practice-answer-btn"
    );


  answerButtons.forEach(answerButton => {
    answerButton.disabled = true;

    const answer = decodeURIComponent(
      answerButton.dataset.answer
    );

    if (answer === question.correct) {
      answerButton.classList.add("correct");
    }
  });


  const feedback =
    currentPracticePanel.querySelector(
      "#practiceFeedback"
    );


  if (selectedAnswer === question.correct) {
    currentPracticeScore++;

    button.classList.add("correct");

    feedback.innerHTML = `
      <div class="feedback-correct">
        😊🎉
      </div>
    `;
  } else {
    button.classList.add("wrong");

    feedback.innerHTML = `
      <div class="feedback-wrong">
       😢
      </div>
    `;
  }


  const nextArea =
    currentPracticePanel.querySelector(
      "#practiceNextArea"
    );

  const isLastQuestion =
  currentPracticeQuestionIndex ===
  currentPracticeQuestions.length - 1;

if (isLastQuestion) {

  // Hiện kết quả sau 1 giây
  setTimeout(() => {
    showLesson7PracticeResult();
  }, 1000);

} else {

  nextArea.innerHTML = `
    <button
      class="practice-next-btn"
      onclick="nextLesson7PracticeQuestion()"
    >
      Next
    </button>
  `;
}
}
function nextLesson7PracticeQuestion() {
  lesson7PracticeAudio.pause();
  lesson7PracticeAudio.currentTime = 0;

  currentPracticeQuestionIndex++;

  if (
    currentPracticeQuestionIndex >=
    currentPracticeQuestions.length
  ) {
    showLesson7PracticeResult();
    return;
  }

  showLesson7PracticeQuestion();
}
function showLesson7PracticeResult() {
  lesson7PracticeAudio.pause();
  lesson7PracticeAudio.currentTime = 0;

  const totalQuestions =
    currentPracticeQuestions.length;

  const percent = Math.round(
    (currentPracticeScore / totalQuestions) * 100
  );

  let resultEmoji = "";
let resultMessage = "";

if (percent === 100) {
    resultEmoji = "🏆";
    resultMessage = "Xuất sắc!";
}
else if (percent >= 80) {
    resultEmoji = "🎉";
    resultMessage = "Tuyệt vời!";
}
else if (percent >= 60) {
    resultEmoji = "👍";
    resultMessage = "Rất tốt!";
}
else {
    resultEmoji = "💪";
    resultMessage = "Cố lên!";
}

  currentPracticePanel.innerHTML = `
    <div class="practice-box">

      <div class="practice-box-title">
        Kết quả luyện tập ${currentPracticeNumber}
      </div>

      <div class="practice-result">

        <div class="practice-result-icon">
          ${resultEmoji}
        </div>

        <div class="practice-result-score">
          ${currentPracticeScore}/${totalQuestions}
        </div>

        <div class="practice-result-message">
          ${resultMessage}
        </div>

        <div class="practice-result-buttons">

          <button
            class="practice-restart-btn"
            onclick="startLesson7Practice()"
          >
            Làm lại
          </button>

          <button
            class="practice-close-btn"
            onclick="closeLesson7Practice()"
          >
            Đóng
          </button>

        </div>

      </div>

    </div>
  `;
}
function closeLesson7Practice() {
  lesson7PracticeAudio.pause();
  lesson7PracticeAudio.currentTime = 0;

  if (!currentPracticePanel) {
    return;
  }

  const practiceItem =
    currentPracticePanel.closest(".practice-item");

  const button =
    practiceItem.querySelector(
      ".practice-title-btn"
    );

  currentPracticePanel.classList.remove("open");
  currentPracticePanel.innerHTML = "";

  button.classList.remove("active");
  button.textContent =
    `▶ Luyện tập ${currentPracticeNumber}`;

  currentPracticePanel = null;
  currentPracticeNumber = null;
}
function playLesson7PracticeQuestionAudio(
  button = null
) {
  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  if (!question || !question.word) {
    return;
  }

  const romaji = question.word.romaji;

  if (!romaji) {
    console.log(
      "Từ này chưa có dữ liệu romaji."
    );

    return;
  }

  lesson7PracticeAudio.pause();
  lesson7PracticeAudio.currentTime = 0;

  lesson7PracticeAudio.src =
    `../../../../assets/audio/lesson7/${romaji}.mp3`;


  if (button) {
    button.classList.add("playing");
  }


  lesson7PracticeAudio
    .play()
    .catch(error => {
      console.log(
        "Không phát được âm thanh:",
        romaji,
        error
      );

      if (button) {
        button.classList.remove("playing");
      }
    });


  lesson7PracticeAudio.onended = () => {
    if (button) {
      button.classList.remove("playing");
    }
  };

  lesson7PracticeAudio.onerror = () => {
    if (button) {
      button.classList.remove("playing");
    }

    console.log(
      `Không tìm thấy file: ${romaji}.mp3`
    );
  };
}

document.addEventListener("DOMContentLoaded", () => {
  renderLesson7Vocab();
  renderLesson7Table();
  initFlashcards();
});