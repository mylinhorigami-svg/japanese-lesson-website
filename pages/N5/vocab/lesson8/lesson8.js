const lesson8Vocab = [
  {
    kana: "たべもの",
    kanji: "食べ物",
    romaji: "tabemono",
    meaning: "Đồ ăn"
  },
  {
    kana: "さくら",
    kanji: "桜",
    romaji: "sakura",
    meaning: "Hoa anh đào"
  },
  {
    kana: "まち",
    kanji: "町",
    romaji: "machi",
    meaning: "Khu phố, thị trấn"
  },
  {
    kana: "やま",
    kanji: "山",
    romaji: "yama",
    meaning: "Núi, ngọn núi"
  },
  {
    kana: "くるま",
    kanji: "車",
    romaji: "kuruma",
    meaning: "Xe hơi"
  }
];
const lesson8Table = [
  {
    kana: "きれい（な）",
    kanji: "",
    romaji: "kirei",
    meaning: "Đẹp, xinh đẹp, sạch sẽ"
  },
  {
    kana: "ハンサム（な）",
    kanji: "",
    romaji: "hansamu",
    meaning: "Đẹp trai"
  },
  {
    kana: "しんせつ（な）",
    kanji: "親切",
    romaji: "shinsetsu",
    meaning: "Tử tế, tốt bụng, thân thiện"
  },
  {
    kana: "ゆうめい（な）",
    kanji: "有名",
    romaji: "yuumei",
    meaning: "Nổi tiếng"
  },
  {
    kana: "げんき（な）",
    kanji: "元気",
    romaji: "genki",
    meaning: "Khỏe mạnh"
  },
  {
    kana: "しずか（な）",
    kanji: "静か",
    romaji: "shizuka",
    meaning: "Yên tĩnh"
  },
  {
    kana: "にぎやか（な）",
    kanji: "",
    romaji: "nigiyaka",
    meaning: "Đông đúc, nhộn nhịp"
  },
  {
    kana: "ひま（な）",
    kanji: "暇",
    romaji: "hima",
    meaning: "Rảnh rỗi"
  },
  
  {
    kana: "べんり（な）",
    kanji: "便利",
    romaji: "benri",
    meaning: "Tiện lợi"
  },
  {
    kana: "すてき（な）",
    kanji: "",
    romaji: "suteki",
    meaning: "Đẹp, tuyệt vời"
  },
  {
    kana: "いそがしい",
    kanji: "忙しい",
    romaji: "isogashii",
    meaning: "Bận rộn"
  },
  {
    kana: "おおきい",
    kanji: "大きい",
    romaji: "ookii",
    meaning: "To, lớn"
  },
  {
    kana: "ちいさい",
    kanji: "小さい",
    romaji: "chiisai",
    meaning: "Nhỏ, bé"
  },
  {
    kana: "あたらしい",
    kanji: "新しい",
    romaji: "atarashii",
    meaning: "Mới"
  },
  {
    kana: "ふるい",
    kanji: "古い",
    romaji: "furui",
    meaning: "Cũ"
  },
  {
    kana: "いい",
    kanji: "良い",
    romaji: "ii",
    meaning: "Tốt, đẹp"
  },
  {
    kana: "わるい",
    kanji: "悪い",
    romaji: "warui",
    meaning: "Tồi, xấu"
  },
  {
    kana: "あつい",
    kanji: "暑い",
    romaji: "atsui",
    meaning: "Nóng (thời tiết)"
  },
  {
    kana: "さむい",
    kanji: "寒い",
    romaji: "samui",
    meaning: "Lạnh, rét"
  },
  {
    kana: "つめたい",
    kanji: "冷たい",
    romaji: "tsumetai",
    meaning: "Lạnh (đồ vật, nhiệt độ、tính cách)"
  },
  {
    kana: "むずかしい",
    kanji: "難しい",
    romaji: "muzukashii",
    meaning: "Khó"
  },
  {
    kana: "やさしい",
    kanji: "優しい",
    romaji: "yasashii",
    meaning: "Dễ, dễ tính"
  },
  {
    kana: "たかい",
    kanji: "高い",
    romaji: "takai",
    meaning: "Đắt, cao"
  },
  {
    kana: "やすい",
    kanji: "安い",
    romaji: "yasui",
    meaning: "Rẻ"
  },
  {
    kana: "ひくい",
    kanji: "低い",
    romaji: "hikui",
    meaning: "Thấp"
  },
  {
    kana: "おもしろい",
    kanji: "",
    romaji: "omoshiroi",
    meaning: "Hay, thú vị"
  },
  {
    kana: "おいしい",
    kanji: "",
    romaji: "oishii",
    meaning: "Ngon"
  },
  {
    kana: "たのしい",
    kanji: "楽しい",
    romaji: "tanoshii",
    meaning: "Vui vẻ"
  },
  {
    kana: "さびしい",
    kanji: "寂しい",
    romaji: "sabishii",
    meaning: "Buồn, cô đơn"
  },
  {
    kana: "しろい",
    kanji: "白い",
    romaji: "shiroi",
    meaning: "Trắng"
  },
  {
    kana: "くろい",
    kanji: "黒い",
    romaji: "kuroi",
    meaning: "Đen"
  },
  {
    kana: "あかい",
    kanji: "赤い",
    romaji: "akai",
    meaning: "Đỏ"
  },
  {
    kana: "あおい",
    kanji: "青い",
    romaji: "aoi",
    meaning: "Xanh"
  },
  {
    kana: "しけん",
    kanji: "試験",
    romaji: "shiken",
    meaning: "Thi, kiểm tra"
  },
  {
    kana: "ところ",
    kanji: "所",
    romaji: "tokoro",
    meaning: "Nơi, chỗ"
  },
  {
    kana: "りょう",
    kanji: "寮",
    romaji: "ryou",
    meaning: "Ký túc xá"
  },
  {
    kana: "せいかつ",
    kanji: "生活",
    romaji: "seikatsu",
    meaning: "Cuộc sống hằng ngày"
  },
  {
    kana: "しごと",
    kanji: "仕事",
    romaji: "shigoto",
    meaning: "Công việc"
  },
  {
    kana: "たいへん",
    kanji: "大変",
    romaji: "taihen",
    meaning: "Cực kỳ, vô cùng; vất vả"
  },
  {
    kana: "とても",
    kanji: "",
    romaji: "totemo",
    meaning: "Rất"
  }
];

// Mảng kết hợp cho flashcard
const allFlashcardData = [...lesson8Vocab, ...lesson8Table];
const lesson8PracticeAllWords = [...lesson8Vocab, ...lesson8Table];

function renderLesson8Vocab() {
  const grid = document.getElementById("lesson8VocabGrid");
  if (!grid) return;

  grid.innerHTML = "";

  lesson8Vocab.forEach(item => {
    const card = document.createElement("div");
    card.className = "vocab-card";

    const kanjiHtml = item.kanji
      ? `<div class="vocab-kanji">${item.kanji}</div>`
      : "";

    card.innerHTML = `
      <img 
        class="vocab-img" 
        src="../../../../assets/images/lesson8/${item.romaji}.jpg" 
        alt="${item.kana}"
      >

      ${kanjiHtml}

      <div class="vocab-kana-row">

    <div class="vocab-kana">
        ${item.kana}
    </div>

    <button class="vocab-sound-btn"
            onclick="playVocabSound('${item.romaji}', this)">
        🔊
    </button>

</div>

      <div class="vocab-romaji">${item.romaji}</div>

      <div class="vocab-meaning">${item.meaning}</div>
    `;

    grid.appendChild(card);
  });
}

function renderLesson8Table() {
  const tableBody = document.getElementById("lesson8TableBody");
  if (!tableBody) return;

  tableBody.innerHTML = "";

  lesson8Table.forEach(item => {
    const row = document.createElement("tr");

    const kanjiCell = item.kanji ? item.kanji : "&nbsp;";

    row.innerHTML = `
      <td class="table-kana">${item.kana}</td>
      <td class="table-kanji">${kanjiCell}</td>
      <td class="table-romaji">${item.romaji}</td>
      <td class="table-meaning">${item.meaning}</td>
      <td>
        <button class="table-sound-btn"
                onclick="playVocabSound('${item.romaji}', this)">
          🔊
        </button>
      </td>
    `;

    tableBody.appendChild(row);
  });
}

function playVocabSound(romaji, btn) {
  const audio = new Audio(`../../../../assets/audio/lesson8/${romaji}.mp3`);

  btn.classList.add("playing");

  audio.play();

  audio.onended = () => {
    btn.classList.remove("playing");
  };

  audio.onerror = () => {
    btn.classList.remove("playing");
    alert("Không tìm thấy file âm thanh: " + romaji + ".mp3");
  };
}

function playFlashcardSound(romaji) {
  if (!romaji) return;

  const audio = new Audio(`../../../../assets/audio/lesson8/${romaji}.mp3`);

  audio.play().catch(() => {
    console.log("Không phát được âm thanh flashcard:", romaji);
  });
}

let flashcardIndex = 0;
let flashcardFlipped = false;
let flashcardVisible = false;

function renderFlashcard() {
  const content = document.getElementById("flashcardContent");
  const hintEl = document.getElementById("flashcardHint");
  const container = document.getElementById("flashcardContainer");

  if (!content || !container || !hintEl) return;
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  const item = allFlashcardData[flashcardIndex];

  flashcardFlipped = false;
  flashcardVisible = true;
  container.hidden = false;
  const counter = document.getElementById("flashcardCounter");

    if (counter) {
    counter.textContent = `${flashcardIndex + 1} / ${allFlashcardData.length}`;
    }
  content.innerHTML = `
    <div class="flashcard-face">
      
      <div class="flashcard-main">
        ${item.kana}
      </div>
    </div>
  `;

  hintEl.textContent = "Nhấn để lật thẻ";

  playFlashcardSound(item.romaji);
}

function toggleFlashcard() {
  const content = document.getElementById("flashcardContent");
  const hintEl = document.getElementById("flashcardHint");

  if (!content || !hintEl) return;
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  const item = allFlashcardData[flashcardIndex];

  if (!flashcardFlipped) {
    content.innerHTML = `
      <div class="flashcard-face">

        <div class="flashcard-kanji">
         ${item.kanji || ""}
        </div>

        <div class="flashcard-kana">
          ${item.kana}
        </div>

        <div class="flashcard-romaji">
          ${item.romaji}
        </div>

        <div class="flashcard-meaning">
          ${item.meaning}
        </div>
      </div>
    `;

    hintEl.textContent = "Nhấn để quay lại";
    flashcardFlipped = true;

  } else {
    content.innerHTML = `
      <div class="flashcard-face">

        <div class="flashcard-main">
          ${item.kana}
        </div>
      </div>
    `;

    hintEl.textContent = "Nhấn để lật thẻ";
    flashcardFlipped = false;
  }
}

function changeFlashcard(step) {
  if (!allFlashcardData || allFlashcardData.length === 0) return;

  flashcardIndex =
    (flashcardIndex + step + allFlashcardData.length) % allFlashcardData.length;

  renderFlashcard();
}

function initFlashcards() {
  const toggleBtn = document.getElementById("flashcardToggleBtn");
  const container = document.getElementById("flashcardContainer");
  const card = document.getElementById("flashcardCard");
  const prevBtn = document.getElementById("flashcardPrev");
  const nextBtn = document.getElementById("flashcardNext");

  if (!toggleBtn || !container || !card || !prevBtn || !nextBtn) return;

  container.hidden = true;

  toggleBtn.addEventListener("click", () => {
    if (flashcardVisible) {
      flashcardVisible = false;
      container.hidden = true;
      toggleBtn.textContent = "🃏 Flashcard";
    } else {
      renderFlashcard();
      toggleBtn.textContent = "🃏 Flashcard";
    }
  });

  card.addEventListener("click", toggleFlashcard);

  card.addEventListener("keydown", (event) => {
    if (event.key === "Enter" || event.key === " ") {
      event.preventDefault();
      toggleFlashcard();
    }

    if (event.key === "ArrowLeft") {
      event.preventDefault();
      changeFlashcard(-1);
    }

    if (event.key === "ArrowRight") {
      event.preventDefault();
      changeFlashcard(1);
    }
  });

  prevBtn.addEventListener("click", (event) => {
    event.stopPropagation();
    changeFlashcard(-1);
  });

  nextBtn.addEventListener("click", (event) => {
    event.stopPropagation();
    changeFlashcard(1);
  });
}

/*Practice*/
const lesson8PracticeWords = {
  1: allFlashcardData.slice(0, 10),
  2: allFlashcardData.slice(10, 20),
  3: allFlashcardData.slice(20, 30),
  4: allFlashcardData.slice(30, 40),
  5: allFlashcardData.slice(40, 49)
};
let currentPracticeNumber = null;
let currentPracticeQuestions = [];
let currentPracticeQuestionIndex = 0;
let currentPracticeScore = 0;
let currentPracticePanel = null;
let lesson8AnswerLocked = false;

const lesson8PracticeAudio = new Audio();
function shuffleLesson8Array(array) {
  const newArray = [...array];

  for (let i = newArray.length - 1; i > 0; i--) {
    const randomIndex = Math.floor(
      Math.random() * (i + 1)
    );

    [newArray[i], newArray[randomIndex]] = [
      newArray[randomIndex],
      newArray[i]
    ];
  }

  return newArray;
}
function createLesson8PracticeAnswers(
  correctWord,
  answerType
) {
  const correctAnswer =
    answerType === "meaning"
      ? correctWord.meaning
      : correctWord.kana;

  const wrongAnswers = allFlashcardData
    .filter(word => {
      return word.kana !== correctWord.kana;
    })
    .map(word => {
      return answerType === "meaning"
        ? word.meaning
        : word.kana;
    })
    .filter((answer, index, array) => {
      return (
        answer &&
        answer !== correctAnswer &&
        array.indexOf(answer) === index
      );
    });

  const selectedWrongAnswers =
    shuffleLesson8Array(wrongAnswers).slice(0, 3);

  return shuffleLesson8Array([
    correctAnswer,
    ...selectedWrongAnswers
  ]);
}
function createLesson8PracticeQuestions(practiceNumber) {
  const words = lesson8PracticeWords[practiceNumber];

  if (!words || words.length === 0) {
    return [];
  }

  const questions = [];

  const shuffledWords = shuffleLesson8Array([
    ...words
  ]);

  const kanaWords = shuffledWords.slice(
    0,
    Math.min(8, shuffledWords.length)
  );

  const meaningWords = shuffleLesson8Array([
    ...words
  ]).slice(
    0,
    Math.min(8, words.length)
  );

  const audioWords = shuffleLesson8Array([
    ...words
  ]).slice(
    0,
    Math.min(4, words.length)
  );

  kanaWords.forEach(word => {
    questions.push({
      type: "kanaToMeaning",
      question: word.kana,
      correct: word.meaning,
      answers: createLesson8PracticeAnswers(
        word,
        "meaning"
      ),
      word: word
    });
  });

  meaningWords.forEach(word => {
    questions.push({
      type: "meaningToKana",
      question: word.meaning,
      correct: word.kana,
      answers: createLesson8PracticeAnswers(
        word,
        "kana"
      ),
      word: word
    });
  });

  audioWords.forEach(word => {
    questions.push({
      type: "audioToMeaning",
      question: "",
      correct: word.meaning,
      answers: createLesson8PracticeAnswers(
        word,
        "meaning"
      ),
      word: word
    });
  });

  return shuffleLesson8Array(questions);
}
function togglePractice(practiceNumber, button) {
  if (!button) {
    const btns = document.querySelectorAll(".practice-title-btn");
    button = btns[Number(practiceNumber) - 1] || btns[0];
  }

  if (!button) return;

  const practiceItem = button.closest(".practice-item");
  if (!practiceItem) return;

  const panel = practiceItem.querySelector(".practice-panel");
  if (!panel) return;

  const isOpen = panel.classList.contains("open");

  lesson8PracticeAudio.pause();
  lesson8PracticeAudio.currentTime = 0;

  document.querySelectorAll(".practice-panel").forEach(item => {
    item.classList.remove("open");
    item.innerHTML = "";
  });

  document.querySelectorAll(".practice-title-btn").forEach((practiceButton, index) => {
    practiceButton.classList.remove("active");
    practiceButton.textContent = `📝 Luyện tập ${index + 1}`;
  });

  if (isOpen) {
    currentPracticePanel = null;
    currentPracticeNumber = null;
    return;
  }

  currentPracticeNumber = Number(practiceNumber);
  currentPracticePanel = panel;

  button.classList.add("active");
  button.textContent = `▼ Luyện tập ${currentPracticeNumber}`;

  panel.classList.add("open");

  startLesson8Practice();
}

function startLesson8Practice() {
  currentPracticeQuestions =
    createLesson8PracticeQuestions(
      currentPracticeNumber
    );

  currentPracticeQuestionIndex = 0;
  currentPracticeScore = 0;
  lesson8AnswerLocked = false;

  showLesson8PracticeQuestion();
}
function showLesson8PracticeQuestion() {
  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  if (!question) {
    showLesson8PracticeResult();
    return;
  }

  lesson8AnswerLocked = false;

  lesson8PracticeAudio.pause();
  lesson8PracticeAudio.currentTime = 0;

  const totalQuestions =
    currentPracticeQuestions.length;

  const questionNumber =
    currentPracticeQuestionIndex + 1;

  const progressPercent =
    (questionNumber / totalQuestions) * 100;


  let instruction = "";

  if (question.type === "kanaToMeaning") {
    instruction =
      "Chọn đáp án đúng";
  }

  if (question.type === "meaningToKana") {
    instruction =
      "Chọn đáp án đúng";
  }

  if (question.type === "audioToMeaning") {
    instruction =
      "Chọn đáp án đúng";
  }


  let questionHTML = `
    <div class="practice-question-text">
      ${question.question}
    </div>
  `;


  if (question.type === "audioToMeaning") {
    questionHTML = `
      <div class="practice-audio-area">

        <button
          class="practice-audio-btn"
          onclick="playLesson8PracticeQuestionAudio(this)"
        >
          <span class="practice-speaker">
            🔊
          </span>
        </button>

      </div>
    `;
  }


  const answersHTML = question.answers
    .map((answer, index) => {
      return `
        <button
          class="practice-answer-btn"
          data-answer="${encodeURIComponent(answer)}"
          onclick="checkLesson8PracticeAnswer(this)"
        >

          <span class="answer-letter">
            ${String.fromCharCode(65 + index)}
          </span>

          <span class="answer-text">
            ${answer}
          </span>

        </button>
      `;
    })
    .join("");


  currentPracticePanel.innerHTML = `
    <div class="practice-box">

      <div class="practice-box-title">
        Từ vựng bài 8
      </div>

      <div class="practice-progress-info">

        <span>
          Câu ${questionNumber}/${totalQuestions}
        </span>

        <span>
          Điểm: ${currentPracticeScore}
        </span>

      </div>

      <div class="practice-progress-track">

        <div
          class="practice-progress-fill"
          style="width: ${progressPercent}%"
        >
        </div>

      </div>

      <div class="practice-content">

        <div class="practice-instruction">
          ${instruction}
        </div>

        ${questionHTML}

        <div class="practice-answer-list">
          ${answersHTML}
        </div>

        <div
          id="practiceFeedback"
          class="practice-feedback"
        >
        </div>

        <div
          id="practiceNextArea"
          class="practice-next-area"
        >
        </div>

      </div>

    </div>
  `;
   if (question.type === "audioToMeaning") {
    setTimeout(() => {
      playLesson8PracticeQuestionAudio();
    }, 500);
  }
}
function checkLesson8PracticeAnswer(button) {
  if (lesson8AnswerLocked) {
    return;
  }

  lesson8AnswerLocked = true;

  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  const selectedAnswer = decodeURIComponent(
    button.dataset.answer
  );

  const answerButtons =
    currentPracticePanel.querySelectorAll(
      ".practice-answer-btn"
    );


  answerButtons.forEach(answerButton => {
    answerButton.disabled = true;

    const answer = decodeURIComponent(
      answerButton.dataset.answer
    );

    if (answer === question.correct) {
      answerButton.classList.add("correct");
    }
  });


  const feedback =
    currentPracticePanel.querySelector(
      "#practiceFeedback"
    );


  if (selectedAnswer === question.correct) {
    currentPracticeScore++;

    button.classList.add("correct");

    feedback.innerHTML = `
      <div class="feedback-correct">
        😊🎉
      </div>
    `;
  } else {
    button.classList.add("wrong");

    feedback.innerHTML = `
      <div class="feedback-wrong">
       😢
      </div>
    `;
  }


  const nextArea =
    currentPracticePanel.querySelector(
      "#practiceNextArea"
    );

  const isLastQuestion =
  currentPracticeQuestionIndex ===
  currentPracticeQuestions.length - 1;

if (isLastQuestion) {

  // Hiện kết quả sau 1 giây
  setTimeout(() => {
    showLesson8PracticeResult();
  }, 1000);

} else {

  nextArea.innerHTML = `
    <button
      class="practice-next-btn"
      onclick="nextLesson8PracticeQuestion()"
    >
      Next
    </button>
  `;
}
}
function nextLesson8PracticeQuestion() {
  lesson8PracticeAudio.pause();
  lesson8PracticeAudio.currentTime = 0;

  currentPracticeQuestionIndex++;

  if (
    currentPracticeQuestionIndex >=
    currentPracticeQuestions.length
  ) {
    showLesson8PracticeResult();
    return;
  }

  showLesson8PracticeQuestion();
}
function showLesson8PracticeResult() {
  lesson8PracticeAudio.pause();
  lesson8PracticeAudio.currentTime = 0;

  const totalQuestions =
    currentPracticeQuestions.length;

  const percent = Math.round(
    (currentPracticeScore / totalQuestions) * 100
  );

  let resultEmoji = "";
let resultMessage = "";

if (percent === 100) {
    resultEmoji = "🏆";
    resultMessage = "Xuất sắc!";
}
else if (percent >= 80) {
    resultEmoji = "🎉";
    resultMessage = "Tuyệt vời!";
}
else if (percent >= 60) {
    resultEmoji = "👍";
    resultMessage = "Rất tốt!";
}
else {
    resultEmoji = "💪";
    resultMessage = "Cố lên!";
}

  currentPracticePanel.innerHTML = `
    <div class="practice-box">

      <div class="practice-box-title">
        Kết quả luyện tập ${currentPracticeNumber}
      </div>

      <div class="practice-result">

        <div class="practice-result-icon">
          ${resultEmoji}
        </div>

        <div class="practice-result-score">
          ${currentPracticeScore}/${totalQuestions}
        </div>

        <div class="practice-result-message">
          ${resultMessage}
        </div>

        <div class="practice-result-buttons">

          <button
            class="practice-restart-btn"
            onclick="startLesson8Practice()"
          >
            Làm lại
          </button>

          <button
            class="practice-close-btn"
            onclick="closeLesson8Practice()"
          >
            Đóng
          </button>

        </div>

      </div>

    </div>
  `;
}
function closeLesson8Practice() {
  lesson8PracticeAudio.pause();
  lesson8PracticeAudio.currentTime = 0;

  if (!currentPracticePanel) {
    return;
  }

  const practiceItem =
    currentPracticePanel.closest(".practice-item");

  const button =
    practiceItem.querySelector(
      ".practice-title-btn"
    );

  currentPracticePanel.classList.remove("open");
  currentPracticePanel.innerHTML = "";

  button.classList.remove("active");
  button.textContent =
    `▶ Luyện tập ${currentPracticeNumber}`;

  currentPracticePanel = null;
  currentPracticeNumber = null;
}
function playLesson8PracticeQuestionAudio(
  button = null
) {
  const question =
    currentPracticeQuestions[
      currentPracticeQuestionIndex
    ];

  if (!question || !question.word) {
    return;
  }

  const romaji = question.word.romaji;

  if (!romaji) {
    console.log(
      "Từ này chưa có dữ liệu romaji."
    );

    return;
  }

  lesson8PracticeAudio.pause();
  lesson8PracticeAudio.currentTime = 0;

  lesson8PracticeAudio.src =
    `../../../../assets/audio/lesson8/${romaji}.mp3`;


  if (button) {
    button.classList.add("playing");
  }


  lesson8PracticeAudio
    .play()
    .catch(error => {
      console.log(
        "Không phát được âm thanh:",
        romaji,
        error
      );

      if (button) {
        button.classList.remove("playing");
      }
    });


  lesson8PracticeAudio.onended = () => {
    if (button) {
      button.classList.remove("playing");
    }
  };

  lesson8PracticeAudio.onerror = () => {
    if (button) {
      button.classList.remove("playing");
    }

    console.log(
      `Không tìm thấy file: ${romaji}.mp3`
    );
  };
}

document.addEventListener("DOMContentLoaded", () => {
  renderLesson8Vocab();
  renderLesson8Table();
  initFlashcards();
});